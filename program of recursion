#include <stdio.h>
int sum(int n);
int main()
{
    int num;
    scanf("%d",&num);
    int s=sum(num);
    printf("%d",s);
    return 0;
}
int sum(int n)
{
    int s;
  if(n!=0)
  return n+ sum(n-1);
  else
  return n;
  
}
